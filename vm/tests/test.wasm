--this is a comment
push 7 -- push 7 to stack
push 3 -- push 3 to stack
add -- add them result will be 10
jmp @LABEL
push 3 -- push 3 to stack
swap -- swap 10 and 3
sub  -- subtract them. result will be 7
dup  -- duplicate 7 and push back to stack
mul -- multiply 7 * 7 = 49
-- the result will be placed on the top of the stack
-- halt will print out the top of the stack
LABEL:
hlt